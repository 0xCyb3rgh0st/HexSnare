{
  "name": "hexsnare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HexSnare - terminal-styled regex forge for log hunters, pentesters, and bug bounty researchers.",
  "keywords": ["regex", "logs", "pentest", "security", "grep", "dfir", "capture-groups"],
  "license": "UNLICENSED",
  "scripts": {
    "dev": "vite --port 1337 --open",
    "build": "vite build",
    "test": "node scripts/smoke.mjs",
    "preview": "vite preview --port 1337"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
